import Phaser from 'phaser';export default class Santa extends Phaser.Sprite {  constructor (game, x, y, asset) {    super(game, x, y, asset);    this.game = game;    this.isClicked = false;    this.anchor.setTo(0.5, 1);    this.inputEnabled = true;    this.events.onInputUp.add(() => {      if (!this.isClicked) {        this.playAnimation();      }    });  }  playAnimation () {    this.isClicked = true;    this.loadTexture('santa-clicked');    const tween = this.game.add.tween(this.scale).to({ y: 0.95}, 200, null, true).repeatAll(2).yoyo(true);    tween.onComplete.add(() => {      this.isClicked = false;      this.loadTexture('santa');    });  }}